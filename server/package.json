{
  "name": "project-journal-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Sync backend for the LVNT project-journal app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
